// Copyright (c) 2010 Peter Klauser

option java_generic_services = true;
option java_package = "com.googlecode.protobuf.pro.duplex.example";
option java_outer_classname = "PingPong";

message Ping {
	required int32 number = 1;
    required bytes pingData = 2;
}

message Pong {
	required int32 number = 1;
    required bytes pongData = 2;
}

service PingService {
    rpc ping(Ping) returns (Pong);
}

service PongService {
    rpc pong(Pong) returns (Ping);
}
